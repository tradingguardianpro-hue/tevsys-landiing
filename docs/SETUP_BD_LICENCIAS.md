# BD licencias — variables de entorno

Para que el webhook persista licencias en Redis, añade en Vercel:

- `UPSTASH_REDIS_REST_URL`
- `UPSTASH_REDIS_REST_TOKEN`

**Guía completa:** Ver en proyecto TGP `docs/SETUP_BD_LICENCIAS_TEVSYS.md` (crear cuenta Upstash, variables, verificación).
