---
import { Header, Footer } from '@components/odyssey-theme';
import Layout from './Base.astro';
import type { Props as BaseHeadProps } from '../components/head/BaseHead.astro';
import settings from '../config/settings';
import { footerNav, footerColumns, footerSocials } from '../config/footer';

export interface Props {
  seo?: BaseHeadProps;
  bodyClass?: string;
}

import { nav } from '../config/nav.js';
import BrandOval from '../components/BrandOval.astro';

const { seo, bodyClass } = Astro.props as Props;
---

<Layout seo={seo} bodyClass={bodyClass}>
  <Header rightMenu navData={nav} slot="header">
    <BrandOval slot="logo" />
  </Header>
  <div id="page">
    <slot />
  </div>
  <Footer
    slot="footer"
    background="var(--theme-surface-1)"
    color="var(--theme-on-surface-1)"
    footerNav={footerNav}
    footerColumns={footerColumns}
    footerSocials={footerSocials}
    copyrightName={settings.name}
    showPlug={settings.showPlug}
  >
    <a href="/" slot="logo" class="footer-brand__name">
    <img src="/favicon.png" alt="" width="24" height="24" class="footer-brand__logo" />
    <span>tevsys™</span>
  </a>
  </Footer>
</Layout>