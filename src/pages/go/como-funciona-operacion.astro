---
---

<!doctype html>
<html lang="es" data-theme="dark">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex,nofollow" />
    <title>Redirigiendo a Operación en curso</title>
    <style>
      html,
      body {
        margin: 0;
        width: 100%;
        height: 100%;
        background: #06080d;
        overflow: hidden;
      }
      .sr-only {
        position: absolute;
        width: 1px;
        height: 1px;
        padding: 0;
        margin: -1px;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        white-space: nowrap;
        border: 0;
      }
    </style>
  </head>
  <body>
    <p class="sr-only">Redirigiendo a Cómo funciona — rama Operación en curso.</p>
    <script>
      import { inject } from '@vercel/analytics';
      inject({
        beforeSend: (event) => {
          if (typeof localStorage !== 'undefined' && localStorage.getItem('va-disable')) {
            return null;
          }
          return event;
        },
      });
      window.setTimeout(() => {
        window.location.replace('/como-funciona#rama-operacion');
      }, 280);
    </script>
    <noscript>
      <meta http-equiv="refresh" content="0;url=/como-funciona#rama-operacion" />
      <a href="/como-funciona#rama-operacion">Ver operación en curso</a>
    </noscript>
  </body>
</html>
